# Overspass

This script is used to collect example data from [overpass](https://overpass-turbo.eu/)
using it's own query language.

Will be generated two files `devices.json` and `buildings.json` that can be used in postman requests to add respective entities.